# Application Environment
PYTHONUNBUFFERED=1
ENVIRONMENT=development

# CORS Configuration
# Use "*" for development, specify domains for production (comma-separated)
# Example for production: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=*

# Database Configuration (PostgreSQL)
INSTANCE_CONNECTION_NAME=postgres:5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=consultancy_pricing

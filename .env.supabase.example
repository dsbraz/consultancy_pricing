# ==============================================
# PRODUÇÃO COM SUPABASE (Servidor Genérico/Docker)
# ==============================================
# Este arquivo é para deploy em produção usando:
# - Docker em servidor próprio (VPS, EC2, etc.) com docker-compose.prod.yml
# - Supabase como banco de dados PostgreSQL gerenciado
#
# Não use este arquivo para Google Cloud Run (veja .env.cloudrun.example)

# Ambiente
ENVIRONMENT=production

# ==============================================
# SUPABASE DATABASE CONFIGURATION
# ==============================================
# Obtenha estas informações no Supabase Dashboard:
# Settings > Database > Connection string > URI

# Database host (formato: db.<seu-projeto>.supabase.co:5432)
INSTANCE_CONNECTION_NAME=db.xxxxxxxxxxxxx.supabase.co:5432

# Database user (padrão é "postgres")
DB_USER=postgres

# Database password (encontrada nas configurações do projeto Supabase)
DB_PASS=sua_senha_do_supabase_aqui

# Database name (padrão é "postgres" para Supabase)
DB_NAME=postgres

# ==============================================
# CORS CONFIGURATION
# ==============================================
# Liste os domínios permitidos (separados por vírgula)
# Exemplos:
#   Domínio único: https://seuapp.com
#   Múltiplos domínios: https://seuapp.com,https://www.seuapp.com
CORS_ORIGINS=https://seuapp.com

# ==============================================
# COMO USAR
# ==============================================
# 1. Copie este arquivo: cp .env.supabase.example .env
# 2. Edite o .env com suas credenciais do Supabase
# 3. Execute: docker-compose -f docker-compose.prod.yml up -d
#
# Para encontrar suas credenciais no Supabase:
#   1. Acesse https://app.supabase.com
#   2. Selecione seu projeto
#   3. Vá em Settings > Database
#   4. Procure "Connection string" > "URI"
#   5. Extraia: host, user, password, database
#
# Veja o guia completo em: DEPLOYMENT.md
